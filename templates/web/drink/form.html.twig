{% extends 'base.html.twig' %}

{% block title %}{{ action_text}} {% endblock %}

{% block body %}
    <div class="container">
        <div class="py-4 text-center">
            <h2>{{ action_text }}</h2>
        </div>

        <div class="row">
            <div class="col-md-12">
                {% include 'web/partials/_message.html.twig' %}

                {{ form(form) }}
            </div>
        </div>
    </div>
{% endblock %}
